 #include <iostream>//a(97)z(122)Z(90)A(65)9(57)0(48)
 #include <string>
#include <vector>
#include <algorithm>
#include <cmath>//pow(a,b);sqrt(a);abs(x)
 using namespace std;
 int main(){


int akki;
cin>>akki;
while (akki--)
{ int n;
    cin>>n;
int k=0, k2=0, c=0, c2=0, c3=0, e=0, e2=0;

    vector <int> a;
    for(int i=0;i<n;i++){
    cin>>k;a.push_back(k);}
        sort(a.begin(),a.end());
        if(n%2==1){
            k = a[n/2];
            for(int i=0;i<n;i++){if(a[i]<k){c++;}if(a[i]>k){c2++;}}
            c3=max(c,c2);
            cout<<c3<<endl;
        }
        else{
             k = a[n/2];
            for(int i=0;i<n;i++){if(a[i]<k){c++;}if(a[i]>k){c2++;}}
            e=max(c,c2);
            c=0;c2=0;
             k = a[(n-1)/2];
            for(int i=0;i<n;i++){if(a[i]<k){c++;}if(a[i]>k){c2++;}}
            e2=max(c,c2);
            c3=min(e,e2);
            cout<<c3<<endl;
           
        }

}

}
   
 




   
                   
    

   
                   
    


   
                   
    
